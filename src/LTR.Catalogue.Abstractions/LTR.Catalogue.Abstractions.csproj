<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>

  <!--
    Contracts only, and deliberately free of any persistence reference. That is what lets the view layer
    depend on the catalogue without depending on a database.
  -->
  <ItemGroup>
    <ProjectReference Include="..\LTR.Core\LTR.Core.csproj" />
  </ItemGroup>

</Project>
